fn main() {
    let logical: bool = true;

    let a_float: f64 = 1.0;
    let an_integer   = 5i32;

    let default_float   = 3.0;
    let default_integer = 7;

    let mut mutable = 12;

    // The type of a variable must be the same type. 
    mutable = 168;
}
